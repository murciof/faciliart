<!DOCTYPE html>
<html>
  <head>
    <title>TccMurciof</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <main class="flex flex-col gap-8 bg-black w-screen h-screen justify-center items-center">
      <div class="text-4xl text-white font-bold">TCC Murcio</div>
      <div class="grid bg-white p-8 rounded-xl w-1/2">
        <%= yield %>
      </div>
    </main>
  </body>
</html>

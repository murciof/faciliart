<!DOCTYPE html>
<html>
  <head>
    <title>TccMurciof</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="font-chivo">
    <main class="flex flex-col-reverse lg:flex-row gap-12 lg:gap-24 bg-black w-screen h-screen justify-center items-center p-24">
      <div class="grid w-full lg:w-1/3">
        <%= yield %>
      </div>
      <div class="hidden lg:flex border-l h-3/4 border-neutral"></div>
      <hr class="flex lg:hidden border-t w-full border-neutral"/>
      <%= link_to root_path do %><%= image_tag "brand.svg", class: "h-16" %><% end %>
    </main>
  </body>
</html>
